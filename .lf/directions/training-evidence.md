# Selected-match training evidence

Before pricing or claiming Allies/Lessons training, bind the complete pipeline
to corrected rules/content, lesson configuration, observation/action ABI and
actual demo checkpoint loading. At the ETU-80 base (1cd60fda), default training,
self-play shard generation, the exp-11 driver and arena play loop use other
decks; selected branch-driver teacher measurements alone are not a production
training receipt. Inspect current consumers before carrying this finding forward.

Measure complete pipeline work on the actual resource profile. Engine-only SPS
is not labels/second or completed games/second. Unknown throughput remains
unknown. Record all attempts, initialization/idle/export costs, and action or
observation overflow. Fixed-size BC trainers must use the admitted observation
shape; matching checkpoint dimensions is not proof of full legal-choice coverage.

Historical experiment bytes and retired task authorization are not reusable
current-world training authority. Keep evaluation deals and training seed
uncertainty separate; do not port claims across worlds.

ETU-79 owns corrected train/export/play receipts after superseding ETU-78.
The planning calculator at `scripts/budget_plan.py` consumes matching receipt
summaries; do not replace missing rates with old-world or engine-only SPS.

Short rental phases must include the provider's minimum billing increment per
lease. Aggregate occupied host-hours alone undercount separate rounded rentals;
keep billed host-hours distinct and include rounding before protecting reserve.
