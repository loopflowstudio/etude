# Etude runtime boundaries

After changing optional advice imports or Study address consumers, use the
focused dependency-blocked play check and Study protocol/runtime checks.
`ed2` identifies a pre-command frame; selected offer and Command validation
belongs on the landmark. Preserve frozen `erd1` evidence and its stronger
command-bound digest. See `etude/README.md` for the runtime conventions.
